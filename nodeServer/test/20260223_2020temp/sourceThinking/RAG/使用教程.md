# 详细教程：本地调用 Qwen3-Reranker-0.6B（Windows 环境下，基于你的 RTX 5070 Ti Laptop 配置）

这个教程基于我们之前的对话和你的本地环境（RTX 5070 Ti Laptop 12GB VRAM, CUDA 12.8, Python 3.13.7, 虚拟环境 `agent-env`），教你如何调用 Qwen3-Reranker-0.6B 模型进行 query-document 相关性打分。重点强调虚拟环境的使用，因为这是避免全局 Python 冲突的关键。

教程包括：
- 虚拟环境激活/管理
- 依赖安装（如果需要重装）
- 代码示例（单条/批量调用 + 速度测试）
- 长上下文处理（你提到的 1700 字场景，估算 6 对/s）
- 优化建议 & 注意事项

## 前提条件
- 你已经创建了虚拟环境 `agent-env`（路径：`E:\AIlibs\agent-env`）
- 安装了 PyTorch 2.10.0+cu128、Flash Attention 2.8.3
- Hugging Face 缓存路径正常（`C:\Users\jbbj\.cache\huggingface\hub`）
- GPU 驱动正常（nvidia-smi 输出你的 5070 Ti）

如果缺少前提，参考我们之前的 MD 文档重新安装。

## 1. 虚拟环境管理（必须先激活！）
虚拟环境是隔离依赖的关键，避免全局 Python 污染。每次调用模型前，都必须进入 `agent-env`。

### 1.1 激活虚拟环境
1. 打开 PowerShell（推荐管理员权限）
2. 切换到项目目录：
   ```
   E:
   cd \AIlibs
   ```
3. 激活：
   ```
   .\agent-env\Scripts\Activate.ps1
   ```
   - 成功后提示符变为 `(agent-env) PS E:\AIlibs>`
   - 如果报错“无法加载文件”（执行策略问题）：运行 `Set-ExecutionPolicy RemoteSigned`（只一次），然后重试。

### 1.2 验证激活成功
```
python -c "import sys; print(sys.executable)"
```
- 正确输出：`E:\AIlibs\agent-env\Scripts\python.exe`
- 错误输出（如 `C:\Python313\python.exe`）：说明没激活 → 重跑 Activate.ps1

### 1.3 退出虚拟环境
```
deactivate
```

### 1.4 常见问题
- **提示符没变**：关闭 PowerShell 重开，或检查路径是否有空格/特殊字符。
- **全局包干扰**：如果激活后还是用全局 python，卸载全局 transformers 等包，或优先用 venv 的 python.exe 运行脚本（见下面）。
- **快捷方式**：新建 `start_env.bat`：
  ```
  @echo off
  E:
  cd \AIlibs
  call agent-env\Scripts\activate.bat
  powershell
  ```
  双击它就能自动激活并进入 PowerShell。

## 2. 依赖安装/检查（在虚拟环境中运行）
激活环境后，检查/安装 reranker 所需包：
```
pip install "transformers>=4.51.0" accelerate
pip show transformers  # 验证版本 >=4.51.0
```

- 如果已安装，跳过。
- 验证 Flash Attention：
  ```
  python -c "import flash_attn; print(flash_attn.__version__)"
  ```
  输出 2.8.3 → OK

## 3. 调用代码示例（单条 & 批量）
reranker 输入：query + doc 字符串  
输出：浮点数（0~1，yes 概率越高越相关）  
速度：在你配置下，短 prompt ~25.57 对/秒，长上下文（1700字 doc）~6 对/秒（估算基于 token 数，实际跑下面代码测）。

### 3.1 单条调用示例（保存为 rerank_single.py）
```
import torch
from transformers import AutoTokenizer, AutoModelForCausalLM

device = "cuda"

# 加载模型
tokenizer = AutoTokenizer.from_pretrained("Qwen/Qwen3-Reranker-0.6B", padding_side="left", trust_remote_code=True)
model = AutoModelForCausalLM.from_pretrained(
    "Qwen/Qwen3-Reranker-0.6B",
    torch_dtype=torch.bfloat16,
    attn_implementation="flash_attention_2",
    device_map="auto",
    trust_remote_code=True
).eval()

yes_id = tokenizer.convert_tokens_to_ids("yes")
no_id = tokenizer.convert_tokens_to_ids("no")

def get_rerank_score(query, doc):
    prompt = f"""<|im_start|>system
Judge whether the Document meets the requirements based on the Query and the Instruct provided. Note that the answer can only be "yes" or "no".<|im_end>
<|im_start|>user
<Instruct>: Given a web search query, retrieve relevant passages that answer the query
<Query>: {query}
<Document>: {doc}<|im_end>
<|im_start|>assistant
<think>

</think>

"""
    inputs = tokenizer(prompt, return_tensors="pt", max_length=8192, truncation=True).to(device)
    with torch.no_grad():
        outputs = model(**inputs)
    logits = outputs.logits[:, -1, :]
    probs = torch.softmax(logits[:, [no_id, yes_id]], dim=-1)
    return probs[0, 1].item()

# 示例使用
query = "新加坡的首都是哪里？"
doc = "新加坡的首都是新加坡市。"
score = get_rerank_score(query, doc)
print(f"相关性分数: {score:.4f}")

# 释放
del model, tokenizer
torch.cuda.empty_cache()
```

运行：`python rerank_single.py`  
输出：相关性分数: 1.0000（或接近1）

### 3.2 批量调用示例（速度更快，适合 rerank top-k 文档）
保存为 rerank_batch.py
```
import torch
from transformers import AutoTokenizer, AutoModelForCausalLM

device = "cuda"

tokenizer = AutoTokenizer.from_pretrained("Qwen/Qwen3-Reranker-0.6B", padding_side="left", trust_remote_code=True)
model = AutoModelForCausalLM.from_pretrained(
    "Qwen/Qwen3-Reranker-0.6B",
    torch_dtype=torch.bfloat16,
    attn_implementation="flash_attention_2",
    device_map="auto",
    trust_remote_code=True
).eval()

yes_id = tokenizer.convert_tokens_to_ids("yes")
no_id = tokenizer.convert_tokens_to_ids("no")

instruction = "Given a web search query, retrieve relevant passages that answer the query"

def get_rerank_scores(query, docs):
    prompts = [
        f"""<|im_start|>system
Judge whether the Document meets the requirements based on the Query and the Instruct provided. Note that the answer can only be "yes" or "no".<|im_end>
<|im_start|>user
<Instruct>: {instruction}
<Query>: {query}
<Document>: {doc}<|im_end>
<|im_start|>assistant
<think>

</think>

""" for doc in docs
    ]
    inputs = tokenizer(prompts, return_tensors="pt", padding=True, max_length=8192, truncation=True).to(device)
    with torch.no_grad():
        outputs = model(**inputs)
    logits = outputs.logits[:, -1, :]
    probs = torch.softmax(logits[:, [no_id, yes_id]], dim=-1)
    return probs[:, 1].tolist()  # list of scores

# 示例使用
query = "新加坡的首都是哪里？"
docs = ["新加坡的首都是新加坡市。", "泰国首都曼谷。", "北京是中国的首都。" * 50]  # 模拟长 doc
scores = get_rerank_scores(query, docs)
for i, score in enumerate(scores):
    print(f"Doc {i+1} 分数: {score:.4f}")

del model, tokenizer
torch.cuda.empty_cache()
```

运行：`python rerank_batch.py`  
- 批量速度会更快（e.g. 3 对 < 50 ms，总吞吐量 > 60 对/秒）

### 3.3 速度测试示例（长上下文 1700字）
你提到 1700 字上下文下 ~6 对/s，我们可以用这个代码测（保存为 rerank_speed_test.py）

```
import torch
import time
from transformers import AutoTokenizer, AutoModelForCausalLM

device = "cuda"

tokenizer = AutoTokenizer.from_pretrained("Qwen/Qwen3-Reranker-0.6B", padding_side="left", trust_remote_code=True)
model = AutoModelForCausalLM.from_pretrained(
    "Qwen/Qwen3-Reranker-0.6B",
    torch_dtype=torch.bfloat16,
    attn_implementation="flash_attention_2",
    device_map="auto",
    trust_remote_code=True
).eval()

yes_id = tokenizer.convert_tokens_to_ids("yes")
no_id = tokenizer.convert_tokens_to_ids("no")

instruction = "Given a web search query, retrieve relevant passages that answer the query"

# 模拟长 doc (1700字，重复填充)
long_doc = "这是一个长文档示例，用于测试长上下文速度。 " * 200  # 约 1700 字

query = "新加坡的首都是哪里？"

prompt = f"""<|im_start|>system
Judge whether the Document meets the requirements based on the Query and the Instruct provided. Note that the answer can only be "yes" or "no".<|im_end>
<|im_start|>user
<Instruct>: {instruction}
<Query>: {query}
<Document>: {long_doc}<|im_end>
<|im_start|>assistant
<think>

</think>

"""

inputs = tokenizer(prompt, return_tensors="pt", max_length=8192, truncation=True).to(device)

# 预热
with torch.no_grad():
    _ = model(**inputs)

# 测速 50 次
times = []
for _ in range(50):
    t0 = time.perf_counter()
    with torch.no_grad():
        outputs = model(**inputs)
    t1 = time.perf_counter()
    times.append(t1 - t0)

avg_time = sum(times) / len(times) * 1000  # ms
throughput = 1000 / avg_time

print(f"长上下文 (1700字) 平均时间: {avg_time:.2f} ms / 对")
print(f"吞吐量: {throughput:.2f} 对/秒")

del model, tokenizer
torch.cuda.empty_cache()
```

运行：`python rerank_speed_test.py`  
- 预计：150–170 ms / 对（约 6 对/秒），匹配你的估算。

## 4. 优化建议 & 注意事项
- **速度优化**：
  - 批量：如 3.2 示例，吞吐量可翻倍。
  - torch.compile：加 `model = torch.compile(model)`（预热后提速 10–30%）
  - 缩短 doc：检索时截断 doc 到 500–1000 token。
- **显存**：长上下文 < 3GB（你 12GB 余裕）。
- **警告忽略**：`torch_dtype deprecated` 改 `dtype=torch.bfloat16`；Flash Attention warning 无害。
- **错误处理**：如果 OOM，改 float16 或截断 max_length=4096。
- **集成到 Agent**：用在 RAG 中，检索后 rerank top-20 → 取 top-5 喂 LLM。
- **测试**：用 nvidia-smi -l 1 监控 GPU。

这个教程自包含，激活环境后直接复制代码就能跑。如果需要扩展（如批量长上下文测试或集成 LanceDB），告诉我！